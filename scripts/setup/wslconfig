# WSL2 メモリ制限（AE 等と共存する場合）
# このファイルを編集し、setup-wslconfig.ps1 で適用
#
# 注意: vhdx は削除しても自動で縮まない。定期的に wsl2-compact.ps1 を実行

[wsl2]
# WSL2 が使用する最大メモリ（AE に十分なメモリを残す）
memory=4GB
# スワップ（メモリ不足時の補助）
swap=2GB
# アイドル時にメモリを Windows に返す（Windows 11 22H2 以降）
autoMemoryReclaim=gradual
