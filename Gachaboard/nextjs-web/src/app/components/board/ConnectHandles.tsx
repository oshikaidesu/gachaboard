"use client";

/**
 * tldraw キャンバス内に配置するボードレベルのコネクトハンドルコンテナ。
 * 各シェイプのハンドルは ShapeConnectHandles（shapes/ 以下）が担当するため、
 * このコンポーネント自体は現在プレースホルダーとして機能する。
 */
export function ConnectHandles() {
  return null;
}
