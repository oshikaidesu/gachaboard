{
  "name": "gachaboard",
  "version": "1.0.0",
  "description": "## Docker で起動（Next.js + Postgres）",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:sync:backend": "node test-sync.mjs",
    "test:sync:ui": "npx playwright install chromium && E2E_BASE_URL=http://localhost:3000 npx playwright test e2e/sync-ui.spec.ts",
    "test:sync:e2e": "E2E_TEST_MODE=1 docker compose up -d --build && npm run test:sync:backend && npm run test:sync:ui"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oshikaidesu/gachaboard.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/oshikaidesu/gachaboard/issues"
  },
  "homepage": "https://github.com/oshikaidesu/gachaboard#readme",
  "dependencies": {
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
