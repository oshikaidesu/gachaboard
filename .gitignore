# 自動生成（setup-tailscale-https.sh で上書き）
Caddyfile

# 起動ロック（二重起動防止）
.gachaboard-start.lock

# 環境変数（シークレット含む）
.env

# 別プロジェクト・アーカイブ（リポジトリには出したくない）
gachaboard-fresh
アーカイブ

# エディタ用（共有不要）
.cursor/

# Node 依存
node_modules/

# sync-server 永続化（SQLite）
nextjs-web/sync-server/sync-data/
