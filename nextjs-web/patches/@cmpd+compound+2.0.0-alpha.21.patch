diff --git a/node_modules/@cmpd/compound/dist-cjs/lib/Compound.js b/node_modules/@cmpd/compound/dist-cjs/lib/Compound.js
index b8fcdf9..8c9ec25 100644
--- a/node_modules/@cmpd/compound/dist-cjs/lib/Compound.js
+++ b/node_modules/@cmpd/compound/dist-cjs/lib/Compound.js
@@ -66,8 +66,17 @@ function Compound(props) {
       }),
       [components]
     ),
-    shapeUtils: (0, import_react.useMemo)(() => [...import_defaultShapeUtils.defaultShapeUtils, ...shapeUtils], [shapeUtils]),
-    tools: (0, import_react.useMemo)(() => [...import_defaultTools.defaultTools, ...import_defaultShapeTools.defaultShapeTools, ...tools], [tools])
+    shapeUtils: (0, import_react.useMemo)(() => {
+      const customTypes = new Set(shapeUtils.map((u) => u.type));
+      const filtered = import_defaultShapeUtils.defaultShapeUtils.filter((u) => !customTypes.has(u.type));
+      return [...filtered, ...shapeUtils];
+    }, [shapeUtils]),
+    tools: (0, import_react.useMemo)(() => {
+      const customIds = new Set(tools.map((t) => t.id));
+      const fd = import_defaultTools.defaultTools.filter((t) => !customIds.has(t.id));
+      const fs = import_defaultShapeTools.defaultShapeTools.filter((t) => !customIds.has(t.id));
+      return [...fd, ...fs, ...tools];
+    }, [tools])
   };
   const assets = (0, import_assetUrls.useDefaultEditorAssetsWithOverrides)(rest.assetUrls);
   const { done: preloadingComplete, error: preloadingError } = (0, import_usePreloadAssets.usePreloadAssets)(assets);
diff --git a/node_modules/@cmpd/compound/dist-cjs/lib/shapes/shared/TextLabel.js b/node_modules/@cmpd/compound/dist-cjs/lib/shapes/shared/TextLabel.js
index e963623..1fae82a 100644
--- a/node_modules/@cmpd/compound/dist-cjs/lib/shapes/shared/TextLabel.js
+++ b/node_modules/@cmpd/compound/dist-cjs/lib/shapes/shared/TextLabel.js
@@ -75,6 +75,7 @@ const TextLabel = import_react.default.memo(function TextLabel2({
       className: "tl-text-label",
       "data-font": font,
       "data-align": align,
+      "data-vertical-align": verticalAlign,
       "data-hastext": !isEmpty,
       "data-isediting": isEditing,
       "data-textwrap": !!wrap,
diff --git a/node_modules/@cmpd/compound/dist-cjs/lib/shapes/shared/useEditableText.js b/node_modules/@cmpd/compound/dist-cjs/lib/shapes/shared/useEditableText.js
index 704c08d..3d45d85 100644
--- a/node_modules/@cmpd/compound/dist-cjs/lib/shapes/shared/useEditableText.js
+++ b/node_modules/@cmpd/compound/dist-cjs/lib/shapes/shared/useEditableText.js
@@ -47,11 +47,16 @@ function useEditableText(id, type, text) {
       if (shape) {
         elm.value = shape.props.text;
         if (elm.value.length && !skipSelect) {
-          elm.select();
+          if (type === "geo") {
+            const len = elm.value.length;
+            elm.setSelectionRange(len, len);
+          } else {
+            elm.select();
+          }
         }
       }
     });
-  }, [editor, id]);
+  }, [editor, id, type]);
   const handleBlur = (0, import_react.useCallback)(() => {
     const ranges = rSelectionRanges.current;
     requestAnimationFrame(() => {
diff --git a/node_modules/@cmpd/compound/dist-cjs/lib/tools/SelectTool/childStates/EditingShape.js b/node_modules/@cmpd/compound/dist-cjs/lib/tools/SelectTool/childStates/EditingShape.js
index 8d30fac..fd42c36 100644
--- a/node_modules/@cmpd/compound/dist-cjs/lib/tools/SelectTool/childStates/EditingShape.js
+++ b/node_modules/@cmpd/compound/dist-cjs/lib/tools/SelectTool/childStates/EditingShape.js
@@ -90,6 +90,8 @@ class EditingShape extends import_editor.StateNode {
                   this.parent.transition("pointing_shape", info);
                   return;
                 }
+              } else if (shape.id === editingShape.id && this.editor.isShapeOfType(shape, "geo")) {
+                return;
               }
             }
           } else {
diff --git a/node_modules/@cmpd/compound/dist-cjs/lib/ui/components/DebugPanel.js b/node_modules/@cmpd/compound/dist-cjs/lib/ui/components/DebugPanel.js
index 640c07f..ddb659a 100644
--- a/node_modules/@cmpd/compound/dist-cjs/lib/ui/components/DebugPanel.js
+++ b/node_modules/@cmpd/compound/dist-cjs/lib/ui/components/DebugPanel.js
@@ -76,18 +76,21 @@ const CurrentState = (0, import_editor.track)(function CurrentState2() {
 });
 const ShapeCount = function ShapeCount2() {
   const editor = (0, import_editor.useEditor)();
+  const msg = (0, import_useTranslation.useTranslation)();
   const count = (0, import_editor.useValue)("rendering shapes count", () => editor.getRenderingShapes().length, [
     editor
   ]);
   return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
     count,
-    " Shapes"
+    " ",
+    msg("debug-panel.shapes")
   ] });
 };
 const DebugMenuContent = (0, import_editor.track)(function DebugMenuContent2({
   renderDebugMenuItems
 }) {
   const editor = (0, import_editor.useEditor)();
+  const msg = (0, import_useTranslation.useTranslation)();
   const { addToast } = (0, import_useToastsProvider.useToasts)();
   const { addDialog } = (0, import_useDialogsProvider.useDialogs)();
   const [error, setError] = React.useState(false);
@@ -166,7 +169,7 @@ const DebugMenuContent = (0, import_editor.track)(function DebugMenuContent2({
               ]
             });
           },
-          label: (0, import_useTranslation.untranslated)("Show toast")
+          label: msg("debug-panel.show-toast")
         }
       ),
       /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
@@ -191,7 +194,7 @@ const DebugMenuContent = (0, import_editor.track)(function DebugMenuContent2({
               }
             });
           },
-          label: (0, import_useTranslation.untranslated)("Show dialog")
+          label: msg("debug-panel.show-dialog")
         }
       ),
       /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
@@ -199,7 +202,7 @@ const DebugMenuContent = (0, import_editor.track)(function DebugMenuContent2({
         {
           type: "menu",
           onClick: () => createNShapes(editor, 100),
-          label: (0, import_useTranslation.untranslated)("Create 100 shapes")
+          label: msg("debug-panel.create-100-shapes")
         }
       ),
       /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
@@ -228,7 +231,7 @@ const DebugMenuContent = (0, import_editor.track)(function DebugMenuContent2({
             }
             window.alert(`Shapes ${shapes.length}, DOM nodes:${descendants}`);
           },
-          label: (0, import_useTranslation.untranslated)("Count shapes / nodes")
+          label: msg("debug-panel.count-shapes-nodes")
         }
       ),
       (() => {
@@ -242,7 +245,7 @@ const DebugMenuContent = (0, import_editor.track)(function DebugMenuContent2({
           onClick: () => {
             setError(true);
           },
-          label: (0, import_useTranslation.untranslated)("Throw error")
+          label: msg("debug-panel.throw-error")
         }
       ),
       /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
@@ -252,7 +255,7 @@ const DebugMenuContent = (0, import_editor.track)(function DebugMenuContent2({
           onClick: () => {
             (0, import_editor.hardResetEditor)();
           },
-          label: (0, import_useTranslation.untranslated)("Hard reset")
+          label: msg("debug-panel.hard-reset")
         }
       )
     ] }),
@@ -309,6 +312,7 @@ function ExampleDialog({
   onContinue
 }) {
   const [dontShowAgain, setDontShowAgain] = React.useState(false);
+  const msg = (0, import_useTranslation.useTranslation)();
   return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
     /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Dialog.Header, { children: [
       /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Dialog.Title, { children: title }),
@@ -323,7 +327,7 @@ function ExampleDialog({
           onClick: () => setDontShowAgain(!dontShowAgain),
           iconLeft: dontShowAgain ? "check" : "checkbox-empty",
           style: { marginRight: "auto" },
-          children: `Don't show again`
+          children: msg("debug-panel.dont-show-again")
         }
       ),
       /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_Button.Button, { type: "normal", onClick: onCancel, children: cancel }),
diff --git a/node_modules/@cmpd/compound/dist-cjs/lib/ui/components/Toolbar/Toolbar.js b/node_modules/@cmpd/compound/dist-cjs/lib/ui/components/Toolbar/Toolbar.js
index 8ee75e5..1c2bb52 100644
--- a/node_modules/@cmpd/compound/dist-cjs/lib/ui/components/Toolbar/Toolbar.js
+++ b/node_modules/@cmpd/compound/dist-cjs/lib/ui/components/Toolbar/Toolbar.js
@@ -234,8 +234,8 @@ function ToolbarButton({
       icon: item.icon,
       "data-state": isSelected ? "selected" : void 0,
       onClick: () => item.onSelect("toolbar"),
-      onTouchStart: (e) => {
-        (0, import_editor.preventDefault)(e);
+      onTouchEnd: (e) => {
+        e.stopPropagation();
         item.onSelect("toolbar");
       }
     }
diff --git a/node_modules/@cmpd/compound/dist-cjs/lib/ui/hooks/useBreakpoint.js b/node_modules/@cmpd/compound/dist-cjs/lib/ui/hooks/useBreakpoint.js
index 879394f..0d07981 100644
--- a/node_modules/@cmpd/compound/dist-cjs/lib/ui/hooks/useBreakpoint.js
+++ b/node_modules/@cmpd/compound/dist-cjs/lib/ui/hooks/useBreakpoint.js
@@ -37,6 +37,7 @@ var import_editor = require("@cmpd/editor");
 var import_react = __toESM(require("react"));
 var import_constants = require("../constants");
 const BreakpointContext = import_react.default.createContext(0);
+const FIXED_DESKTOP_BREAKPOINT = 6;
 function BreakPointProvider({
   forceMobile = false,
   children
@@ -45,14 +46,17 @@ function BreakPointProvider({
   const breakpoint = (0, import_editor.useValue)(
     "breakpoint",
     () => {
-      const { width } = editor.getViewportScreenBounds();
-      const maxBreakpoint = forceMobile ? 3 : import_constants.PORTRAIT_BREAKPOINTS.length - 1;
-      for (let i = 0; i < maxBreakpoint; i++) {
-        if (width > import_constants.PORTRAIT_BREAKPOINTS[i] && width <= import_constants.PORTRAIT_BREAKPOINTS[i + 1]) {
-          return i;
+      if (forceMobile) {
+        const { width } = editor.getViewportScreenBounds();
+        const maxBreakpoint = 3;
+        for (let i = 0; i < maxBreakpoint; i++) {
+          if (width > import_constants.PORTRAIT_BREAKPOINTS[i] && width <= import_constants.PORTRAIT_BREAKPOINTS[i + 1]) {
+            return i;
+          }
         }
+        return maxBreakpoint;
       }
-      return maxBreakpoint;
+      return FIXED_DESKTOP_BREAKPOINT;
     },
     [editor]
   );
diff --git a/node_modules/@cmpd/compound/dist-esm/lib/Compound.mjs b/node_modules/@cmpd/compound/dist-esm/lib/Compound.mjs
index ba5fdee..6dfcf77 100644
--- a/node_modules/@cmpd/compound/dist-esm/lib/Compound.mjs
+++ b/node_modules/@cmpd/compound/dist-esm/lib/Compound.mjs
@@ -54,8 +54,17 @@ function Compound(props) {
       }),
       [components]
     ),
-    shapeUtils: useMemo(() => [...defaultShapeUtils, ...shapeUtils], [shapeUtils]),
-    tools: useMemo(() => [...defaultTools, ...defaultShapeTools, ...tools], [tools])
+    shapeUtils: useMemo(() => {
+      const customTypes = new Set(shapeUtils.map((u) => u.type));
+      const filtered = defaultShapeUtils.filter((u) => !customTypes.has(u.type));
+      return [...filtered, ...shapeUtils];
+    }, [shapeUtils]),
+    tools: useMemo(() => {
+      const customIds = new Set(tools.map((t) => t.id));
+      const fd = defaultTools.filter((t) => !customIds.has(t.id));
+      const fs = defaultShapeTools.filter((t) => !customIds.has(t.id));
+      return [...fd, ...fs, ...tools];
+    }, [tools])
   };
   const assets = useDefaultEditorAssetsWithOverrides(rest.assetUrls);
   const { done: preloadingComplete, error: preloadingError } = usePreloadAssets(assets);
diff --git a/node_modules/@cmpd/compound/dist-esm/lib/shapes/shared/TextLabel.mjs b/node_modules/@cmpd/compound/dist-esm/lib/shapes/shared/TextLabel.mjs
index dc1ff57..d40c9f6 100644
--- a/node_modules/@cmpd/compound/dist-esm/lib/shapes/shared/TextLabel.mjs
+++ b/node_modules/@cmpd/compound/dist-esm/lib/shapes/shared/TextLabel.mjs
@@ -45,6 +45,7 @@ const TextLabel = React.memo(function TextLabel2({
         className: "tl-text-label",
         "data-font": font,
         "data-align": align,
+        "data-vertical-align": verticalAlign,
         "data-hastext": !isEmpty,
         "data-isediting": isEditing,
         "data-textwrap": !!wrap,
diff --git a/node_modules/@cmpd/compound/dist-esm/lib/shapes/shared/useEditableText.mjs b/node_modules/@cmpd/compound/dist-esm/lib/shapes/shared/useEditableText.mjs
index 80f042c..93fb7cc 100644
--- a/node_modules/@cmpd/compound/dist-esm/lib/shapes/shared/useEditableText.mjs
+++ b/node_modules/@cmpd/compound/dist-esm/lib/shapes/shared/useEditableText.mjs
@@ -30,11 +30,16 @@ function useEditableText(id, type, text) {
       if (shape) {
         elm.value = shape.props.text;
         if (elm.value.length && !skipSelect) {
-          elm.select();
+          if (type === "geo") {
+            const len = elm.value.length;
+            elm.setSelectionRange(len, len);
+          } else {
+            elm.select();
+          }
         }
       }
     });
-  }, [editor, id]);
+  }, [editor, id, type]);
   const handleBlur = useCallback(() => {
     const ranges = rSelectionRanges.current;
     requestAnimationFrame(() => {
diff --git a/node_modules/@cmpd/compound/dist-esm/lib/tools/SelectTool/childStates/EditingShape.mjs b/node_modules/@cmpd/compound/dist-esm/lib/tools/SelectTool/childStates/EditingShape.mjs
index 8076054..6d4e553 100644
--- a/node_modules/@cmpd/compound/dist-esm/lib/tools/SelectTool/childStates/EditingShape.mjs
+++ b/node_modules/@cmpd/compound/dist-esm/lib/tools/SelectTool/childStates/EditingShape.mjs
@@ -69,6 +69,8 @@ class EditingShape extends StateNode {
                   this.parent.transition("pointing_shape", info);
                   return;
                 }
+              } else if (shape.id === editingShape.id && this.editor.isShapeOfType(shape, "geo")) {
+                return;
               }
             }
           } else {
diff --git a/node_modules/@cmpd/compound/dist-esm/lib/ui/components/DebugPanel.mjs b/node_modules/@cmpd/compound/dist-esm/lib/ui/components/DebugPanel.mjs
index 6de59f8..ec68c65 100644
--- a/node_modules/@cmpd/compound/dist-esm/lib/ui/components/DebugPanel.mjs
+++ b/node_modules/@cmpd/compound/dist-esm/lib/ui/components/DebugPanel.mjs
@@ -52,18 +52,21 @@ const CurrentState = track(function CurrentState2() {
 });
 const ShapeCount = function ShapeCount2() {
   const editor = useEditor();
+  const msg = useTranslation();
   const count = useValue("rendering shapes count", () => editor.getRenderingShapes().length, [
     editor
   ]);
   return /* @__PURE__ */ jsxs("div", { children: [
     count,
-    " Shapes"
+    " ",
+    msg("debug-panel.shapes")
   ] });
 };
 const DebugMenuContent = track(function DebugMenuContent2({
   renderDebugMenuItems
 }) {
   const editor = useEditor();
+  const msg = useTranslation();
   const { addToast } = useToasts();
   const { addDialog } = useDialogs();
   const [error, setError] = React.useState(false);
@@ -142,7 +145,7 @@ const DebugMenuContent = track(function DebugMenuContent2({
               ]
             });
           },
-          label: untranslated("Show toast")
+          label: msg("debug-panel.show-toast")
         }
       ),
       /* @__PURE__ */ jsx(
@@ -167,7 +170,7 @@ const DebugMenuContent = track(function DebugMenuContent2({
               }
             });
           },
-          label: untranslated("Show dialog")
+          label: msg("debug-panel.show-dialog")
         }
       ),
       /* @__PURE__ */ jsx(
@@ -175,7 +178,7 @@ const DebugMenuContent = track(function DebugMenuContent2({
         {
           type: "menu",
           onClick: () => createNShapes(editor, 100),
-          label: untranslated("Create 100 shapes")
+          label: msg("debug-panel.create-100-shapes")
         }
       ),
       /* @__PURE__ */ jsx(
@@ -204,7 +207,7 @@ const DebugMenuContent = track(function DebugMenuContent2({
             }
             window.alert(`Shapes ${shapes.length}, DOM nodes:${descendants}`);
           },
-          label: untranslated("Count shapes / nodes")
+          label: msg("debug-panel.count-shapes-nodes")
         }
       ),
       (() => {
@@ -218,7 +221,7 @@ const DebugMenuContent = track(function DebugMenuContent2({
           onClick: () => {
             setError(true);
           },
-          label: untranslated("Throw error")
+          label: msg("debug-panel.throw-error")
         }
       ),
       /* @__PURE__ */ jsx(
@@ -228,7 +231,7 @@ const DebugMenuContent = track(function DebugMenuContent2({
           onClick: () => {
             hardResetEditor();
           },
-          label: untranslated("Hard reset")
+          label: msg("debug-panel.hard-reset")
         }
       )
     ] }),
@@ -287,6 +290,7 @@ function ExampleDialog({
   onContinue
 }) {
   const [dontShowAgain, setDontShowAgain] = React.useState(false);
+  const msg = useTranslation();
   return /* @__PURE__ */ jsxs(Fragment, { children: [
     /* @__PURE__ */ jsxs(Dialog.Header, { children: [
       /* @__PURE__ */ jsx(Dialog.Title, { children: title }),
@@ -301,7 +305,7 @@ function ExampleDialog({
           onClick: () => setDontShowAgain(!dontShowAgain),
           iconLeft: dontShowAgain ? "check" : "checkbox-empty",
           style: { marginRight: "auto" },
-          children: `Don't show again`
+          children: msg("debug-panel.dont-show-again")
         }
       ),
       /* @__PURE__ */ jsx(Button, { type: "normal", onClick: onCancel, children: cancel }),
diff --git a/node_modules/@cmpd/compound/dist-esm/lib/ui/components/Toolbar/Toolbar.mjs b/node_modules/@cmpd/compound/dist-esm/lib/ui/components/Toolbar/Toolbar.mjs
index 885ebe7..dc101e9 100644
--- a/node_modules/@cmpd/compound/dist-esm/lib/ui/components/Toolbar/Toolbar.mjs
+++ b/node_modules/@cmpd/compound/dist-esm/lib/ui/components/Toolbar/Toolbar.mjs
@@ -201,8 +201,8 @@ function ToolbarButton({
       icon: item.icon,
       "data-state": isSelected ? "selected" : void 0,
       onClick: () => item.onSelect("toolbar"),
-      onTouchStart: (e) => {
-        preventDefault(e);
+      onTouchEnd: (e) => {
+        e.stopPropagation();
         item.onSelect("toolbar");
       }
     }
diff --git a/node_modules/@cmpd/compound/dist-esm/lib/ui/hooks/useBreakpoint.mjs b/node_modules/@cmpd/compound/dist-esm/lib/ui/hooks/useBreakpoint.mjs
index 5fb002d..891b2b3 100644
--- a/node_modules/@cmpd/compound/dist-esm/lib/ui/hooks/useBreakpoint.mjs
+++ b/node_modules/@cmpd/compound/dist-esm/lib/ui/hooks/useBreakpoint.mjs
@@ -3,6 +3,7 @@ import { useEditor, useValue } from "@cmpd/editor";
 import React, { useContext } from "react";
 import { PORTRAIT_BREAKPOINTS } from "../constants.mjs";
 const BreakpointContext = React.createContext(0);
+const FIXED_DESKTOP_BREAKPOINT = 6;
 function BreakPointProvider({
   forceMobile = false,
   children
@@ -11,14 +12,17 @@ function BreakPointProvider({
   const breakpoint = useValue(
     "breakpoint",
     () => {
-      const { width } = editor.getViewportScreenBounds();
-      const maxBreakpoint = forceMobile ? 3 : PORTRAIT_BREAKPOINTS.length - 1;
-      for (let i = 0; i < maxBreakpoint; i++) {
-        if (width > PORTRAIT_BREAKPOINTS[i] && width <= PORTRAIT_BREAKPOINTS[i + 1]) {
-          return i;
+      if (forceMobile) {
+        const { width } = editor.getViewportScreenBounds();
+        const maxBreakpoint = 3;
+        for (let i = 0; i < maxBreakpoint; i++) {
+          if (width > PORTRAIT_BREAKPOINTS[i] && width <= PORTRAIT_BREAKPOINTS[i + 1]) {
+            return i;
+          }
         }
+        return maxBreakpoint;
       }
-      return maxBreakpoint;
+      return FIXED_DESKTOP_BREAKPOINT;
     },
     [editor]
   );
diff --git a/node_modules/@cmpd/compound/src/lib/Compound.tsx b/node_modules/@cmpd/compound/src/lib/Compound.tsx
index bcbc697..6f2d07e 100644
--- a/node_modules/@cmpd/compound/src/lib/Compound.tsx
+++ b/node_modules/@cmpd/compound/src/lib/Compound.tsx
@@ -86,8 +86,19 @@ export function Compound(props: CompoundProps) {
 			}),
 			[components]
 		),
-		shapeUtils: useMemo(() => [...defaultShapeUtils, ...shapeUtils], [shapeUtils]),
-		tools: useMemo(() => [...defaultTools, ...defaultShapeTools, ...tools], [tools]),
+		shapeUtils: useMemo(() => {
+			const customTypes = new Set(shapeUtils.map((u) => (u as { type: string }).type))
+			const filtered = defaultShapeUtils.filter(
+				(u) => !customTypes.has((u as { type: string }).type)
+			)
+			return [...filtered, ...shapeUtils]
+		}, [shapeUtils]),
+		tools: useMemo(() => {
+			const customIds = new Set(tools.map((t) => (t as { id: string }).id))
+			const fd = defaultTools.filter((t) => !customIds.has((t as { id: string }).id))
+			const fs = defaultShapeTools.filter((t) => !customIds.has((t as { id: string }).id))
+			return [...fd, ...fs, ...tools]
+		}, [tools]),
 	}
 
 	const assets = useDefaultEditorAssetsWithOverrides(rest.assetUrls)
diff --git a/node_modules/@cmpd/compound/src/lib/ui/components/DebugPanel.tsx b/node_modules/@cmpd/compound/src/lib/ui/components/DebugPanel.tsx
index 0147fba..2d3a4ac 100644
--- a/node_modules/@cmpd/compound/src/lib/ui/components/DebugPanel.tsx
+++ b/node_modules/@cmpd/compound/src/lib/ui/components/DebugPanel.tsx
@@ -70,11 +70,12 @@ const CurrentState = track(function CurrentState() {
 
 const ShapeCount = function ShapeCount() {
 	const editor = useEditor()
+	const msg = useTranslation()
 	const count = useValue('rendering shapes count', () => editor.getRenderingShapes().length, [
 		editor,
 	])
 
-	return <div>{count} Shapes</div>
+	return <div>{count} {msg('debug-panel.shapes')}</div>
 }
 
 const DebugMenuContent = track(function DebugMenuContent({
@@ -83,6 +84,7 @@ const DebugMenuContent = track(function DebugMenuContent({
 	renderDebugMenuItems: (() => React.ReactNode) | null
 }) {
 	const editor = useEditor()
+	const msg = useTranslation()
 	const { addToast } = useToasts()
 	const { addDialog } = useDialogs()
 	const [error, setError] = React.useState<boolean>(false)
@@ -167,7 +169,7 @@ const DebugMenuContent = track(function DebugMenuContent({
 							],
 						})
 					}}
-					label={untranslated('Show toast')}
+					label={msg('debug-panel.show-toast')}
 				/>
 
 				<DropdownMenu.Item
@@ -190,12 +192,12 @@ const DebugMenuContent = track(function DebugMenuContent({
 							},
 						})
 					}}
-					label={untranslated('Show dialog')}
+					label={msg('debug-panel.show-dialog')}
 				/>
 				<DropdownMenu.Item
 					type="menu"
 					onClick={() => createNShapes(editor, 100)}
-					label={untranslated('Create 100 shapes')}
+					label={msg('debug-panel.create-100-shapes')}
 				/>
 				<DropdownMenu.Item
 					type="menu"
@@ -227,7 +229,7 @@ const DebugMenuContent = track(function DebugMenuContent({
 
 						window.alert(`Shapes ${shapes.length}, DOM nodes:${descendants}`)
 					}}
-					label={untranslated('Count shapes / nodes')}
+					label={msg('debug-panel.count-shapes-nodes')}
 				/>
 				{(() => {
 					if (error) throw Error('oh no!')
@@ -237,14 +239,14 @@ const DebugMenuContent = track(function DebugMenuContent({
 					onClick={() => {
 						setError(true)
 					}}
-					label={untranslated('Throw error')}
+					label={msg('debug-panel.throw-error')}
 				/>
 				<DropdownMenu.Item
 					type="menu"
 					onClick={() => {
 						hardResetEditor()
 					}}
-					label={untranslated('Hard reset')}
+					label={msg('debug-panel.hard-reset')}
 				/>
 			</DropdownMenu.Group>
 			<DropdownMenu.Group>
@@ -324,6 +326,7 @@ function ExampleDialog({
 	onContinue: () => void
 }) {
 	const [dontShowAgain, setDontShowAgain] = React.useState(false)
+	const msg = useTranslation()
 
 	return (
 		<>
@@ -340,7 +343,7 @@ function ExampleDialog({
 						iconLeft={dontShowAgain ? 'check' : 'checkbox-empty'}
 						style={{ marginRight: 'auto' }}
 					>
-						{`Don't show again`}
+						{msg('debug-panel.dont-show-again')}
 					</Button>
 				)}
 				<Button type="normal" onClick={onCancel}>
diff --git a/node_modules/@cmpd/compound/src/lib/ui/components/Toolbar/Toolbar.tsx b/node_modules/@cmpd/compound/src/lib/ui/components/Toolbar/Toolbar.tsx
index c900b2b..342e30c 100644
--- a/node_modules/@cmpd/compound/src/lib/ui/components/Toolbar/Toolbar.tsx
+++ b/node_modules/@cmpd/compound/src/lib/ui/components/Toolbar/Toolbar.tsx
@@ -262,8 +262,8 @@ function ToolbarButton({
 			icon={item.icon}
 			data-state={isSelected ? 'selected' : undefined}
 			onClick={() => item.onSelect('toolbar')}
-			onTouchStart={(e) => {
-				preventDefault(e)
+			onTouchEnd={(e) => {
+				e.stopPropagation()
 				item.onSelect('toolbar')
 			}}
 		/>
